{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build lab1",
            "type": "shell",
            "command": "gcc",
            "args": [
                "-o", "Lab1.exe", "${workspaceFolder}/lab1.c"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}