{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build lab3",
            "type": "shell",
            "command": "gcc",
            "args": [
                "-o", "Lab3.exe", "${workspaceFolder}/Lab3.c", "-lm"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}