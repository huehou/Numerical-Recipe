{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build trial",
            "type": "shell",
            "command": "gcc",
            "args": [
                "-o", "trial.exe", "${workspaceFolder}/Trial/trial.c"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}